# Supabase — necessário para o cliente em src/lib/supabase.js
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Asaas — usado futuramente pelas Edge Functions (create-order, asaas-webhook).
# NUNCA expor essas chaves com o prefixo VITE_ (ficariam públicas no bundle do frontend).
# ASAAS_API_KEY=
# ASAAS_WEBHOOK_TOKEN=
